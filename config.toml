[server]
    port = "8000"
    imagePath = "./images"
    host = "http://localhost:8000"

[postgres]
    user = "postgres"
    password = "example"
    host = "db"
    port = "5432"
    dbname = "kanban-board"
